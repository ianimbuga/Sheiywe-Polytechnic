// JavaScript to handle dynamic actions
document.addEventListener('DOMContentLoaded', function () {
    const admissionsSection = document.getElementById('admissions');

    // Display an alert when the "Admissions" section is clicked
    admissionsSection.addEventListener('click', function () {
        alert('Applications are now open! Apply today.');
    });
});
